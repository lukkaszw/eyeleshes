import * as user from './user.actionCreators';
import * as clients from './clients.actionCreators';
import * as visits from './visits.actionCreators';

const ACTION_CREATORS = {
  user,
  clients,
  visits,
};

export default ACTION_CREATORS;