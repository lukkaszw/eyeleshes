import * as user from './user.selectors';
import * as clients from './clients.selectors';
import * as visits from './visits.selectors';

const SELECTORS = {
  user,
  clients,
  visits,
};

export default SELECTORS;