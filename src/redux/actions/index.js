import * as user from './user.actions';
import * as clients from './clients.actions';
import * as visits from './visits.actions';

const ACTIONS = {
  user,
  clients,
  visits,
};

export default ACTIONS;